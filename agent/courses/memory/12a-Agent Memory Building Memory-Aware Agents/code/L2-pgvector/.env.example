# PostgreSQL 连接串（默认对应 README 里的 pg-memory-lab 容器，5433 避开本机 5432）
PG_DSN=postgresql://postgres:postgres@127.0.0.1:5433/postgres

# HuggingFace 镜像（datasets 拉数据集用；main.py 里已有 setdefault，可不设）
# HF_ENDPOINT=https://hf-mirror.com
