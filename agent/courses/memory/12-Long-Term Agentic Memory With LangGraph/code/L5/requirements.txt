# python package requirements file
#
# 本地化版（2026-07）：课程原 pin（langchain 0.3.18 / langgraph 0.2.72 / langmem 0.0.8 等）
# 已升级到当时最新版本，并追加 DeepSeek + fastembed 本地演示栈所需依赖。
# 建议 python 3.11：uv venv --python 3.11 .venv && uv pip install -r requirements.txt
# L2-L5 共用 code/ 根目录这一个 venv；模型与 key 配置见 code/.env（DEEPSEEK_API_KEY / MODEL）。

langchain==1.3.14
langchain-openai==1.4.1
langchain-anthropic==1.5.2
langgraph==1.2.9
langmem==0.0.30
fastembed==0.8.0
python-dotenv==1.0.1
ipykernel
nbconvert
ipywidgets
