# 12b L5 本地演示：Letta server + client + 本地 embedding 服务
# python 3.11
#
# 注意：letta 0.6.50 声明 typer<0.10，resolver 一次装不下来，必须分两步
# （第二步强制覆盖 click/typer，否则 letta CLI 起不来）：
#   uv pip install --python .venv/bin/python letta==0.6.50 letta-client==0.1.324 fastembed python-dotenv
#   uv pip install --python .venv/bin/python click==8.1.7 typer==0.12.5
letta==0.6.50
letta-client==0.1.324
fastembed
python-dotenv
click==8.1.7
typer==0.12.5
