# 可选环境变量(都有合理默认,不设也能跑)

# 基座模型。默认 SmolLM2-360M(本机缓存里唯一权重完整的生成模型)。
# 网络允许时换成更强、自带 chat 模板的 instruct 小模型:
# BASE_MODEL=Qwen/Qwen2.5-0.5B-Instruct
# BASE_MODEL=meta-llama/Llama-3.2-1B-Instruct   # 与课程最贴近,但 HF 上 gated,需登录

# 训练设备。默认 CPU —— Apple Silicon 上 MPS 跑长训练会把整机搞崩重启(见 README 踩坑)。
# 有 NVIDIA 卡:TRAIN_DEVICE=cuda
# TRAIN_DEVICE=cpu
