# L7 依赖 —— 单个 3.12 venv,服务器与客户端共用(Presidio 在客户端进程内也要跑)。
#
# ⚠️ 沿用 L3/L5/L6 的坑:
#   1. guardrails 只支持 Python <3.13 → 用 3.12 建 .venv-guardrails。
#   2. `guardrails start` 自举装 guardrails-api 版本号非法 → 预装 guardrails-api==0.0.1。
#   3. click 8.2+ 与 guardrails 0.5.3 CLI 冲突 → 钉死 click<8.2(装完其它依赖后再压一次)。
#   4. 服务器 config 用同步 Guard(非 AsyncGuard),见 L3。
#
# 装完依赖后,还需装 spacy 英文模型(Presidio 默认用 en_core_web_lg):
#   .venv-guardrails/bin/python -m spacy download en_core_web_lg
# 若被网络限制,直接装 wheel:
#   .venv-guardrails/bin/pip install \
#     https://github.com/explosion/spacy-models/releases/download/en_core_web_lg-3.8.0/en_core_web_lg-3.8.0-py3-none-any.whl

# guardrails 本体 + 服务器
guardrails-ai==0.5.3
guardrails-api==0.0.1
click<8.2

# Microsoft Presidio(本地 PII 引擎,逐字照课程)+ spacy
presidio-analyzer
presidio-anonymizer
spacy

# RAG 应用侧 + 客户端
openai>=1.30
fastembed>=0.3
numpy
httpx>=0.27
python-dotenv>=1.0
