# L5 依赖 —— 单个 3.12 venv,**服务器和客户端共用**(Part 1 的 Guard 在客户端进程内跑,
# 也要 ML 栈;Part 2 的服务器同样要)。所以这里把 L3(服务器)+ L4(NLI 栈)的依赖合在一起。
#
# ⚠️ 沿用 L3/L4 踩过的坑:
#   1. guardrails 只支持 Python <3.13 → 用 3.12 建 .venv-guardrails。
#   2. `guardrails start` 自举装 guardrails-api 的版本号非法 → 预装 guardrails-api==0.0.1。
#   3. click 8.2+ 与 guardrails 0.5.3 的 CLI 冲突 → 钉死 click<8.2。
#      注意:装 torch/transformers/guardrails-api 都会把 click 顶回新版,最后要再压一次:
#        .venv-guardrails/bin/pip install "click<8.2"
#   4. 服务器 config 用同步 Guard(非 AsyncGuard),见 L3 说明。
#
# 首次运行下载 NLI 模型 + all-MiniLM(约 1.3G/百 MB);国内可 export HF_ENDPOINT=https://hf-mirror.com
# nltk punkt_tab 由 ensure_punkt() 自动下载。

# guardrails 本体 + 服务器
guardrails-ai==0.5.3
guardrails-api==0.0.1
click<8.2

# 幻觉 validator 的 NLI 栈(逐字照课程)
torch
transformers
sentence-transformers
nltk

# RAG 应用侧 + 客户端
openai>=1.30
fastembed>=0.3
numpy
httpx>=0.27
python-dotenv>=1.0
