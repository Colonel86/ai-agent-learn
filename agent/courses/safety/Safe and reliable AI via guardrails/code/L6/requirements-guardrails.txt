# L6 依赖 —— 单个 3.12 venv,服务器和客户端共用(分类器在客户端进程内也要跑)。
#
# ⚠️ 沿用 L3/L5 踩过的坑:
#   1. guardrails 只支持 Python <3.13 → 用 3.12 建 .venv-guardrails。
#   2. `guardrails start` 自举装 guardrails-api 版本号非法 → 预装 guardrails-api==0.0.1。
#   3. click 8.2+ 与 guardrails 0.5.3 CLI 冲突 → 钉死 click<8.2(装完 torch/transformers 后再压一次)。
#   4. 服务器 config 用同步 Guard(非 AsyncGuard),见 L3 说明。
#
# 首次运行下载 facebook/bart-large-mnli(约 1.6G);国内可 export HF_ENDPOINT=https://hf-mirror.com

# guardrails 本体 + 服务器
guardrails-ai==0.5.3
guardrails-api==0.0.1
click<8.2

# zero-shot 话题分类器(逐字照课程)
torch
transformers

# RAG 应用侧 + 客户端
openai>=1.30
fastembed>=0.3
numpy
httpx>=0.27
python-dotenv>=1.0
